import { TemplatedApp } from 'uWebSockets.js';
// import { index } from '../controllers/home/homeController';

export const homeRouter = (app: TemplatedApp): void => {
  // app.get('/*', index); TODO: implement handling this route
};
